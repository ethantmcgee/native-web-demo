import { LitElement, html, css } from 'lit';
import { property, customElement } from 'lit/decorators.js';

const SEARCH_WORKER = "YOUR_WORKER_URL_HERE";

@customElement('search-base')
export class NativeWebDemo extends LitElement {
  render() {
    return html`
      
    `;
  }
}
